import React,{useEffect,useState} from 'react'; import {createRoot} from 'react-dom/client'; import './style.css';
function App(){const [books,setBooks]=useState([]);const [q,setQ]=useState('');const [cat,setCat]=useState('');const load=()=>fetch(`/api/books?q=${encodeURIComponent(q)}&category=${encodeURIComponent(cat)}`).then(r=>r.json()).then(setBooks);useEffect(()=>{load()},[]);return <div><header><h1>📚 BookStore</h1><p>React frontend for the Spring Boot BookStore API</p></header><main><div className="search"><input placeholder="Search" value={q} onChange={e=>setQ(e.target.value)}/><input placeholder="Category" value={cat} onChange={e=>setCat(e.target.value)}/><button onClick={load}>Search</button></div><section>{books.map(b=><article key={b.id}><h2>{b.name}</h2><p>{b.author} · {b.category}</p><p>{b.description}</p><strong>₹{b.price}</strong><p>Stock: {b.stock}</p></article>)}</section></main></div>}createRoot(document.getElementById('root')).render(<App/>);
